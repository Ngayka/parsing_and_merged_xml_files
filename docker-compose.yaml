version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis
    restart: unless-stopped

  celery-worker:
    build: .
    env_file: .env
    command: celery -A celery_app worker -l info
    volumes:
      - ./data:/data
      - ./:/app
      - ./path/to/GoogleDrive/XML_merge:/data #вот тут потрібно додати свій шлях до диску
    depends_on:
      - redis
    restart: unless-stopped

  celery-beat:
    build: .
    env_file:
      - .env
    command: celery -A celery_app beat -l info
    volumes:
      - ./data:/data
      - ./:/app
      - ./path/to/GoogleDrive/XML_merge:/data #вот тут потрібно додати свій шлях до диску
    depends_on:
      - redis
    restart: unless-stopped

  nginx:
    image: nginx:latest
    volumes:
      - data:/data
      - ./:/app
      - ./path/to/GoogleDrive/XML_merge:/data #вот тут потрібно додати свій шлях до диску
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    restart: unless-stopped

volumes:
  data:

